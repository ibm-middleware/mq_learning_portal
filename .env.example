# MQ Native HA Portal — example environment (no secrets)
PORT=5080
HOST=0.0.0.0
FLASK_DEBUG=false

# Branding overrides (generic defaults only — do not commit real org names if private)
PORTAL_ORG=Enterprise Messaging
PORTAL_SITE_NAME=MQ Native HA Portal

# Optional custom site config path
# SITE_CONFIG=site.yaml
